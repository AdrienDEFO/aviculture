% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/decision.R
\name{simulate_market_flow}
\alias{simulate_market_flow}
\title{Simulate Discrete Poultry Flow with Mass and Volume}
\usage{
simulate_market_flow(
  start_time = 0,
  end_time = 60,
  time_step = 1,
  model = NULL,
  entry = 0,
  entry_is_rate = TRUE,
  mortality_rate = 0.01,
  ms,
  vs,
  demand = NULL,
  demand_is_rate = TRUE,
  feed_price = 0,
  bird_price = 0,
  daily_feed = 0,
  fixed_cost_per_day = 0,
  stagnation_window = 3,
  min_mass_growth = 0.002,
  min_volume_growth = 0.002,
  return_history = FALSE
)
}
\arguments{
\item{start_time}{Numeric. Start time (days). Default 0.}

\item{end_time}{Numeric. End time (days). Default 60.}

\item{time_step}{Numeric. Time step in days. Default 1.}

\item{model}{A model object from \code{\link{load_model}}. If NULL, loads default.}

\item{entry}{Numeric, vector, or function. Entry flow of chicks. If vector, aligned with time steps.}

\item{entry_is_rate}{Logical. If TRUE, entry is per-day rate and will be multiplied by time_step.}

\item{mortality_rate}{Numeric or function(age, mass, volume, time) giving per-day mortality rate.}

\item{ms}{Numeric. Minimum marketable mass (kg).}

\item{vs}{Numeric. Minimum marketable volume (L).}

\item{demand}{Numeric, vector, or function. Market demand (birds per day).}

\item{demand_is_rate}{Logical. If TRUE, demand is per-day rate and will be multiplied by time_step.}

\item{feed_price}{Numeric. Feed price per kg.}

\item{bird_price}{Numeric. Sale price per bird.}

\item{daily_feed}{Numeric. Feed consumption per bird per day (kg).}

\item{fixed_cost_per_day}{Numeric. Fixed cost per day.}

\item{stagnation_window}{Integer. Window size for stagnation detection. Default 3.}

\item{min_mass_growth}{Numeric. Minimum average mass growth per step. Default 0.002.}

\item{min_volume_growth}{Numeric. Minimum average volume growth per step. Default 0.002.}

\item{return_history}{Logical. If TRUE, returns cohort history.}
}
\value{
A list with:
\item{summary}{Data frame by time: population, marketable, stagnant, sold, entered, deaths}
\item{cohorts}{Final cohort table}
\item{history}{Cohort history if return_history=TRUE}
}
\description{
Discrete-time simulation aligned with Mod210126: tracks cohorts by age,
mass, and volume; controls entry flow; counts marketable birds; flags stagnation.
}
\examples{
\dontrun{
model <- load_model()
sim <- simulate_market_flow(
  end_time = 60, time_step = 1,
  entry = 100, entry_is_rate = FALSE,
  ms = 2.0, vs = 2.0,
  demand = 80, demand_is_rate = FALSE,
  model = model
)
head(sim$summary)
}

}
