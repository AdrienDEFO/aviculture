% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/decision.R
\name{identify_stagnant}
\alias{identify_stagnant}
\title{Identify Birds (or Cohorts) with Stagnant Growth}
\usage{
identify_stagnant(
  history,
  id_col = "id",
  time_col = "time",
  mass_col = "mass",
  volume_col = "volume",
  window = 3,
  min_mass_growth = 0.002,
  min_volume_growth = 0.002
)
}
\arguments{
\item{history}{Data frame with columns for time, mass, volume, and id.}

\item{id_col}{Character. Column name for bird or cohort id. Default "id".}

\item{time_col}{Character. Column name for time. Default "time".}

\item{mass_col}{Character. Column name for mass. Default "mass".}

\item{volume_col}{Character. Column name for volume. Default "volume".}

\item{window}{Integer. Number of last time steps to evaluate. Default 3.}

\item{min_mass_growth}{Numeric. Minimum average mass growth per time step. Default 0.002.}

\item{min_volume_growth}{Numeric. Minimum average volume growth per time step. Default 0.002.}
}
\value{
Data frame with one row per id and columns:
\item{id}{Bird or cohort id}
\item{stagnant}{TRUE if growth is below thresholds}
\item{mass_growth}{Average mass growth per step}
\item{volume_growth}{Average volume growth per step}
}
\description{
Flags birds or cohorts that are no longer growing based on recent
mass and volume changes.
}
\examples{
\dontrun{
hist <- data.frame(id = c(1,1,1,2,2,2),
                   time = c(1,2,3,1,2,3),
                   mass = c(1.0,1.01,1.01,1.0,1.05,1.10),
                   volume = c(0.9,0.91,0.91,0.9,0.96,1.02))
identify_stagnant(hist, window = 2, min_mass_growth = 0.005, min_volume_growth = 0.005)
}

}
