% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model.R
\name{run_aviculture_simulation}
\alias{run_aviculture_simulation}
\title{Run a Single Population Simulation}
\usage{
run_aviculture_simulation(
  initial_population = 100,
  days = 35,
  feed_price = 300,
  bird_price = 2500,
  daily_feed = 0.03,
  daily_deaths = 0.01,
  model = NULL,
  optav_enabled = FALSE,
  model_type = "standard",
  dt = NULL,
  demand = NULL,
  demand_profile = NULL,
  objective = c("profit", "satisfaction", "balanced"),
  method = c("euler", "rk4")
)
}
\arguments{
\item{initial_population}{Numeric. Starting flock size. Default: 100.}

\item{days}{Numeric. Simulation duration in days. Default: 35.}

\item{feed_price}{Numeric. Feed cost (currency per kg). Default: 300.}

\item{bird_price}{Numeric. Sale price (currency per bird). Default: 2500.}

\item{daily_feed}{Numeric. Feed consumption per bird per day (kg). Default: 0.03.}

\item{daily_deaths}{Numeric. Baseline mortality rate (daily fraction). Default: 0.01.}

\item{model}{A model object from \code{\link{load_model}}. If NULL, loads default.}

\item{optav_enabled}{Logical. If TRUE, use optimized OPTAV parameters. Default: FALSE.}

\item{model_type}{Character. "standard" or other variants. Default: "standard".}

\item{dt}{Numeric. Time step in days (small for stability). Default: 0.1.}

\item{demand}{Numeric. Demand rate (birds per day) or vector. Used if demand_profile is NULL.}

\item{demand_profile}{Function or numeric vector providing demand at time t.}

\item{objective}{Character. Objective: "profit", "satisfaction", or "balanced".}

\item{method}{Character. Numerical scheme: "euler" (default) or "rk4".}
}
\value{
A data frame with columns:
\item{time}{Day (0 to days)}
\item{population}{Living birds at time t}
\item{average_mass}{Mean mass per bird (kg)}
\item{total_mass}{Total flock mass (kg)}
\item{marketable_population}{Estimated marketable birds at time t}
\item{economic_potential}{Economic potential estimate at time t}
\item{cumulative_feed_cost}{Total feed cost to date}
\item{cumulative_profit}{Net profit to date}
\item{growth_rate}{Population derivative (birds/day)}
}
\description{
Simulates a flock of broilers over time using explicit Euler or RK4 schemes.
Accounts for growth, mortality, and sales dynamics.
}
\details{
Uses explicit Euler by default with a small time step (default 0.1 day).
Optionally uses RK4 if \code{method="rk4"}.
Population dynamics:
dN/dt = births - deaths - sales

OPTAV model adjusts feed efficiency and reduces mortaliy through
optimized management practices.
}
\examples{
\dontrun{
  model <- load_model()
  sim <- run_aviculture_simulation(
    initial_population = 100,
    days = 35,
    feed_price = 300,
    bird_price = 2500,
    model = model
  )
  
  plot(sim$time, sim$population, type = 'l',
       xlab = "Days", ylab = "Population",
       main = "Broiler Flock Size Over Time")
  
  plot(sim$time, sim$cumulative_profit, type = 'l',
       xlab = "Days", ylab = "Profit",
       main = "Cumulative Profit")
}

}
