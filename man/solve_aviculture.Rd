% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/solver_pde.R
\name{solve_aviculture}
\alias{solve_aviculture}
\title{Solve the poultry farm PDE system}
\usage{
solve_aviculture(
  grid = create_grid(),
  parameters = create_parameters(),
  controls = create_controls(grid = grid),
  initial_healthy = NULL,
  initial_stunted = NULL,
  store_states = TRUE
)
}
\arguments{
\item{grid}{An object created with \code{\link[=create_grid]{create_grid()}}.}

\item{parameters}{An object created with \code{\link[=create_parameters]{create_parameters()}}.}

\item{controls}{An object created with \code{\link[=create_controls]{create_controls()}}.}

\item{initial_healthy}{Optional initial healthy stock matrix of dimension
\verb{length(grid$age) x length(grid$mass)}.}

\item{initial_stunted}{Optional initial stunted stock matrix of dimension
\verb{length(grid$age) x length(grid$mass)}.}

\item{store_states}{Logical; if \code{TRUE}, keeps the full state arrays.}
}
\value{
An object of class \code{aviculture_simulation}.
}
\description{
Solves the age-mass structured poultry model with an explicit upwind finite
difference scheme. The default configuration uses the degree-4 healthy growth
polynomial estimated from the project regression script and the stunted
growth rule \code{gamma_2 = rho * gamma_1}.
}
